# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Defaults so the kernel boots outside docker-compose (e.g. in CI).
# Real environment variables (docker) and phpunit.dist.xml overrides win.
DEFAULT_URI=http://localhost
MAILER_DSN=null://null
RECAPTCHA_SECRET_KEY=
CONTACT_RECIPIENT_EMAIL=test@clapiersvb.fr
CONTACT_SENDER_EMAIL=test@clapiersvb.fr

# HelloAsso — valeurs factices : le client est stubé dans les tests (zéro réseau).
HELLOASSO_BASE_URL=https://api.helloasso-sandbox.com
HELLOASSO_CLIENT_ID=test-client-id
HELLOASSO_CLIENT_SECRET=test-client-secret
HELLOASSO_ORGANIZATION_SLUG=test-asso
HELLOASSO_MEMBERSHIP_FORM_TYPE=Membership
HELLOASSO_MEMBERSHIP_FORM_SLUG=test-form
